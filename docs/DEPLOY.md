# Deploying Porterage

Two keys, both kept on this computer, never in the repo and never printed:

| Key | File | Does | Command |
|---|---|---|---|
| Contract deployer (Ethereum) | `~/.config/porterage/deploy-key` | Deploys and wires the contracts | `npm run deploy-key`, then `npm run deploy` |
| Name key (sr25519 mnemonic) | `~/.config/porterage/name-key` | Owns `porterage.dot` and sets what it points to | `npm run name-key`, then `npm run deploy:app` |

They are separate because pad signs sr25519 from a mnemonic, and the contracts
were deployed from an Ethereum key. The phone is not involved in either: pad's
phone signer is a hidden product account that has moved between key schemes and
stranded names before (sonde, 2026-09-18).

`npm run deploy:app` checks the key and the name, refuses if `PRODUCT_ID` in
`web/src/config.ts` differs from the label, runs the web tests and build, and
publishes `web/dist` with pad as a library. The name key signs DotNS; one of
pad's pool accounts signs the Bulletin upload. `--check` stops before anything is
signed. Registering an unowned label needs `--register` and the label typed back
(or `CONFIRM_LABEL=<label>.dot`).

## Record

| Date | What | Detail |
|---|---|---|
| 2026-09-19 | Contracts on Paseo Asset Hub | Deployer `0x4B38b09Fb2c7B5310f4f7D51d359Fc63a344c420`, about 9 PAS. Addresses in `deployed-addresses.json`. |
| 2026-09-19 | Registered `porterage.dot` | Owner: the name key, `5GVobt2azMJ3PjZxtc935wJJL3aDEM9cQoSU3C8d12ZrxVtu` / `0xc05764d1eea94660ec073ab288f5cd0bf5d85061`. 11 PAS paid. Funded with 20 PAS from sonde's deploy key. |
| 2026-09-19 | Published | `bafybeihmklscserz4j6pwpbwhhooktramdsfqopwuz62x5qtjjcmwodo6y`. The first run registered the name, but its contenthash step timed out after 300 s; the re-run set it. |
| 2026-09-19 | Published the private balance screen | `bafybeiewfiinn276mrigc22j53c3ubqhhh4umwplluvf67djozvs5hin5e` |
| 2026-09-19 | Published withdrawals and the funding market, with the 32.8 MiB proving key | `bafybeicbqnb4d5awqi57cillg72drnfmrlhyxeo73bhav6xelhzacdhtfi`. 38.6 MB in 32 Bulletin chunks; the publish took 9 min 51 s. |
| 2026-09-19 | Wired shielded payouts and published them | Shield verifier deployed and keyed, vault pointed at Kusama Shield, Poseidon and the buckets; all 22 checks pass. App `bafybeigrpxwlcsuupapakqsnc7qi6y2wejk3mdagcftt3h6dak4hrcwebm` (8.1 MB incremental). |
| 2026-09-20 | Published token top-ups | `bafybeic4rvhmfgqar7lazji2d5x24jpmdr3lyz4a567j77ekwnkbvoceii` (1.5 MB incremental) |
| 2026-09-20 | Published ordering, venues and the auction | `bafybeif2e3e4rmjvfpaiuc5uszchc4wihvh2efzvv3vjf7p5jzbhzjiike` |
| 2026-09-20 | Published the QR handoffs and the proximity proof | `bafybeiaztukjm2b3rdfir6wd2iyfohjwvhpus6jxl5q2caomsgaxad464q` |
| 2026-09-20 | Published menus and photo evidence | `bafybeicefnhlydlxbxkxckqywpz4yog5k4yhssb4nexnhhay7j24nde6ne` |
| 2026-09-20 | Published the kitchen view | `bafybeicr7f34iysz3r6nv77ficonpxy5ayfzu6epqlgqr7ai42ps4whgce` |
| 2026-09-20 | Published the drop map, and buttons in place of dropdowns | `bafybeib2lzzmx5mj7vjhy3bydmhgwy5xd2les5zuxb5hphwwbyjg2lxs7a`. The app's WebView never opened `<select>` popups, so the venue and token lists couldn't be picked from on a phone. |
| 2026-09-20 | Published order messages | `bafybeigmc2cq5vncrwrjtvmmk5qiculiuqiawamwo4uruiebz7bnjlr2ne` |
| 2026-09-20 | Set the arbiter, published disputes and ratings | `bafybeid3puptsivrvxoxfdpwzcxq6rmxiss5simvvb6mu53lcie3qrob5e`. Arbiter = the deploy key `0x4B38…c420`; its public key is in `web/src/deployed.json` and the app refuses it unless it hashes to that address. Dispute bond 0 (bootstrap). |
| 2026-09-20 | Topped the deployer up | 120 PAS from the name key, in two transfers (`node tools/fund-deployer.mjs`). Live runs now refund their throwaway accounts, so one costs about 3.5 PAS instead of 30. |
| 2026-09-20 | Published the operations console | `bafybeig6l26y3o4utzieunckbcjjvl3cct3qik4xxz7pnymeqp7hdg4eei`. Read-only unless the device holds the arbiter key; `npx vite-node tools/ops.ts -- list \| show \| rule` is the arbiter's command line. |
| 2026-09-20 | Published live chat and notifications | `bafybeihhtroi3bp6jwpbd3xtgvskebb5air2tt7momctyzsceymfnqumxm`. `npm run test:rtc` checks the signalling rebuild against a real WebRTC stack. |
| 2026-09-20 | Published distance filtering | `bafybeia66uhny2pqkocs74jglbesixhy3q2utlz43suinbdheue5hpzbhq`. Saved pin never leaves the device; the opt-in drop area is a fixed ~1.1 km grid cell. |
| 2026-09-20 | Published thread archives | `bafybeie4opv7id6dcwyzav4fhjb4zo5nyvdkn56qdwfyz5lp4nqemwymuy`. A long conversation's transcript goes to Bulletin once per windowful, not per message. |
| 2026-09-20 | Published the drop hand-off | `bafybeia74t5ffmzn37xu5dilebflhqx6g7ntdzqjxt7oru6rsmnfmh2spu`. The chosen driver is sent the exact drop, sealed to it alone. |
| 2026-09-20 | Published directions | `bafybeibmmnyth46txr3spc6lj5xqqiocsrb5iigxcorz3vbtvhjamt2oma`. `geo:` through `navigateTo`, then a web map or the clipboard by choice. |
| 2026-09-20 | Published menu caching and venue labels | `bafybeifr3pmtpygxyn5mzrofds2kxzr5j3ogce5vukm24ota56snmrs7ri` |
| 2026-09-20 | Published the visual pass | `bafybeihvfjquhslmygvlmu3lc3oakqidt7cwhwmc3nareu7avegrn5drmi`. Type and space scale, tokens with dark values, marked primary actions, host theme followed. |
| 2026-09-20 | Published the picker module | `bafybeihdflnqtdgyoge5kjmb73m7biwwrv2iugebo2zlprnwef5j2jyigu`. One amount parser, presets, steppers, validation beside the field. |
| 2026-09-20 | Published the smaller items | `bafybeibd7ezbdy6ylpbwbqxcyjgqys75tzruejp47ekb4pv2qxk4vk6paa`. Order history, a "what happens next" line per role, half-failed sends retried when the order is reopened, the venue's rating and takings on one screen, and the privacy copy gathered into `copy/privacy.ts`. |
| 2026-09-20 | Published the phone probes | `bafybeih575ildphdbz54i5swwhfga25nwuln4z3dp2w2wpwukmwwqoyqby`. "Check this phone" under the role chooser: the four unmeasured questions, each as a run of a couple of minutes, with a report to paste back. |
| 2026-09-21 | Published the phone measurements | `bafybeifolluevfo5h4nhl6vucqs5cozs3y3dd6ksae3gpilraxczbzwv4u`. `geo:` confirmed working (51 ms, map app opened, app survived); a Bulletin write costs a prompt every time and took 31.5 s then 5.6 s, so the thread archive came off the message path and became a asked-for action; notifications arrive while backgrounded. The camera question is reopened — `QrScan` now says whether a code came by camera or by paste. |
| 2026-09-21 | Published the customer flow, rebuilt | `bafybeieofp5puazjvfwyabj345siq3etkct4zn73ccdrxu24gtz5shteeq`. Browse → store → bag → place, one step at a time; address search, venue tiles with photos and price tiers, menu sections, vendor tax lines. The 1,040-line order screen is 704 and only owns the live order. |
| 2026-09-21 | Published the live order: voice, trip distance, counter photo | `bafybeigiwzizinq73lttp6i6vjnhh5uju44jdxugupd2t4c35gjtas3xqm`. Voice negotiated on every WebRTC connection (226 B sealed, still inside a 512 B statement); the delivery distance shown above the bids; a photo at the counter kept on the phone and committed with the door photo under the single evidence key the contract allows. |
| 2026-09-21 | Published the driver and vendor flows | `bafybeidvjgsdqvcinlx7suceb76sodtfr2i4rc6rzcdbzdc3su4ct5trjq`. Driver: Work / Yours / Earnings / Setup, job cards leading with the fare and both distances. Vendor: Counter / Menu / Takings / Setup, opening on the counter. |
| 2026-09-21 | Redeployed PorterOrders for `reopenTimedOut` | Orders `0x29834483AaC84767415Fa0a7A22E0c96c3f27f04` → **`0x11cB9aEE722384B80bB80c714669d30Bf552bb14`**. Surgical: `orders` removed from the address book and the deploy re-run, so every `configure`/`setAuthorized` guard compared against the new address and rewired itself. **Venues and drivers survived** — only order state resets, which is unavoidable. All 23 validation checks pass. |
| 2026-09-21 | Published driver profiles, late-driver replacement | `bafybeicylevn5b76mz32ksbg5tiire5z2ebc4unixan4tb7rla4iod57ie`. Bids carry a name, vehicle and rating; the driver's photo is encrypted once and its key sealed per order to the customer who picked them; a late driver can be replaced without losing the order; losing bidders are told. |
| 2026-09-21 | Redeployed PorterVault shielded-only | Vault `0xe70ADDFDf619Bbc4f2500bb1e5840E839448910e` → **`0x21A7F0C49b4cd0B03ae4DD63df462722f12D0fDE`**. The six named-address exits now sit behind `clearExitsOpen`, which ships **false** and is confirmed false on-chain; `insertShieldNote` is never gated, so money can always leave, just not in the clear. **The old vault's 23.275 PAS is stranded** — 1.0 PAS of shield buffer, two inserted notes, and six venue payouts whose keys were `Wallet.createRandom()` in `web/tools/live-order.ts` and are gone. The deployer's 0.225 PAS was recovered first (tx `0x6e3e1e32335f949e881a1e3bd153a8252700a3bfc07c9ed2d887be7a23b7b89a`). All 25 validation checks pass. |
| 2026-09-21 | Fixed a latent redeploy hazard in `scripts/deploy.ts` | `orders.configure` was guarded on `settlement` alone and `disputes.configure` on `orders` alone, so a vault-only redeploy would have skipped both and left orders **and** disputes crediting the dead vault. Validation checked `orders.vault` but never `disputes.vault`. Both guards now compare every address their call writes, and both vault checks are in validation. Rehearsed on a local node before the live run. |
| 2026-09-21 | Published the shielded-only vault | `bafybeidjmtugxsppvlci2p6skw33iz3lze4j3em7g6a77urotdnu2baueu`. The app points at the new vault; the dead one appears nowhere in the bundle. `node tools/costs.mjs` prints the per-role cost and the effective service fee. |
| 2026-09-21 | Fixed `missing provider` on every driver write | `bafybeig3fuqq54rcey7oeqny24aanlq3otuavu7j43ar3wui5psrfhv7la`. Tapping **Bid** on a phone died with `missing provider (operation="sendTransaction")`: `keys.ts` derives the session key with `new Wallet(hex)`, which signs but cannot send, and `Driver.tsx` passed it down unconnected. Bidding, `confirmPickup` and `commitEvidence` were all broken the same way, invisibly — a `Wallet` with no provider is still a `Wallet`. All six signer-bound contracts now build through `writable()` in `contracts.ts`, and `contracts.test.ts` reads the source and fails if a seventh one skips it. |
| 2026-09-21 | Deployed the fee market (all three phases) | Vault `0x21A7…0fDE` → **`0x013fd0C18f8EaC04Cb700330fDDC1A14ca833Bf3`**, orders `0x11cB…bb14` → **`0x48222a180DFd2D41505bD20a93a2C6B06334BBBA`**, settlement `0x4B70…1556` → **`0x9344B26D4204EA0f52E1A6b133cF06e1709cFB9F`**. Vault gains `tip()` (permissionless credit, so a burner pays a submitter INTO the shielded rail) and a signed `maxFee` on `insertShieldNoteFor`/`…TokenFor`. Orders gains `relayFeeAt`/`setRelayFeeCurve`: the escrowed service fee is now a ceiling and the relay earns the price at the moment it settled, clocked from the driver's signed door timestamp. `clearExitsOpen` confirmed still false on the new vault; the curve reads 3750 bps over 30 s on-chain. All 25 checks pass. **Live order state reset** — unavoidable when orders redeploys; venues and drivers survived. |
| 2026-09-21 | Published the fee market | `bafybeihgwh53e75td2lzrsx5oo7twjwxzv7zb3ry6jdkghedsajlw56aza`. Withdrawal requests carry an auction schedule instead of a flat 0.3 PAS tip; submitters re-price as the offer climbs and claim a job before spending gas on it; fees are paid into the vault so a submitter's income shields like anyone's earnings. |
| 2026-09-21 | In and out ramps for every role | `bafybeiefru46f4hmqyrlie2ji622tt4hmioyrdv43uiykyngpdnslknhtm`. Drivers and venues could shield earnings and then do nothing with them: `fundBurner` lived only in `Wallet.tsx` and only the customer rendered it. A shared `Funds` panel (add / take out) now sits in the driver's Earnings, the venue's Takings and ops, and the treasury — which collects every protocol fee and had no shielded path at all — gets the same, read-only unless the device holds that key. **A cash-out stops at an unlinked account and stays there**; sending it on to a named wallet is a separate button with the cost written next to it, because that hop is what publishes the amount against a name. |
| 2026-09-21 | The books: receipts, accounting and export | `bafybeihf3zqanwl2btmsgbeo4se4ctaao4fgdovpfy7piyzitul3hzthki`. Vendors had **no records at all** — baskets lived in React state fed by statements that expire in about an hour, and the chain keeps the money but not the items. The sharp end was tax: `orderValue` is goods **plus** the vendor's named tax lines, so a venue is paid the tax it must remit, and nothing recorded how much of it was tax; since the split is computed on the customer's device from the menu of that moment, a later price edit made it unrecoverable. `books/ledger.ts` now freezes the bill as charged (items, unit prices, each tax line with its bps, totals) for sales, driver earnings and customer receipts, encrypted under its own key. `books/csv.ts` exports two shapes — one row per order for the accounts, one per line item for stock — in decimal PAS so a spreadsheet can't mangle 18 digits. Venues and drivers can close a day to an encrypted Bulletin backup (one prompt, batched, because a write was measured at 6–31 s); **customer receipts stay on the device and are never backed up**, with a delete button. |
| 2026-09-21 | Withdrawals span several notes | `bafybeihuwgcihn4ysicccxsexfcuwnmne6lsdtav43lbzmgs356yytsezq`. Reported from a phone: 57.61 PAS shielded, asked for 25, told *"no single note holds 26.6 PAS"*. A withdrawal spends exactly ONE note and adds a fee on top, so with buckets of 1/5/25/100 **asking for a round bucket amount could never succeed** — the fee always pushed it past the largest note. `shield/plan.ts` now selects several notes, accounting for the fact that the target grows with the plan (k notes = k fees), and `fundBurner` runs them in sequence into the same account. Both money screens show the real ceiling (`maxWithdrawable`) and an "All of it" button instead of letting someone walk into the wall. A multi-note plan says plainly that those notes become visibly one person's. |
| 2026-09-21 | Split the relay key; backup for the note book | `bafybeidxtzfzfnnkaf7zgztf34c72to3kgxtvobzxwh4tmunnrd33iv3ym`. The relay defaulted to `~/.config/porterage/deploy-key` — which is **also the treasury, the arbiter and the upgrade authority** — and it is the only one of those that runs unattended on a networked machine. `npm run relay-key -- --fund 20` makes it its own key; the relay now runs on `0x99139eE45115062544CFb374Dde92924162267c0` and warns loudly if it falls back to the deploy key. Separately, the **books** had an encrypted backup before the **money** did: `shield/recover.ts` backs the note book up to Bulletin under the account's own derived key, and restore **merges** — spent is one-way, so an old backup can never resurrect a spent note and publish its nullifier for nothing. |
| 2026-09-21 | An ops surface, and a way to see the market | `bafybeiejlw3d4tcdivcdop27v444jem2cdbioshnohzdurjodfl7nons3a`. `PorterPauseRegistry` and `PorterGovernanceRouter` had existed since the first deploy with **no screen at all** — stopping anything meant a script, a terminal and the deploy key, which is exactly what nobody has to hand when they need to stop something. `views/Controls.tsx` shows each category's state and who this device is (guardian can pause alone and instantly; **only the owner can unpause**, so the button asks first), plus the router's current addresses read-only. `views/MarketHealth.tsx` + `ops/market.ts` answer "is anyone submitting?" by matching posted requests against `Withdrawal` events: it tells an empty market (nothing clearing) apart from one refused request (others clearing, that one isn't) apart from a healthy one. |
| 2026-09-24 | Built the contract migration path | `bafybeiasnzh4rlzywj3doxxmzunmxz3dqmkplpeaf64begcgt5c4nc5zhq`. The freeze-and-drain model was stated in `PorterUpgradable.migrate` and half built: Drivers and Venues could copy records, **nothing called those copies**, Ratings could not copy at all, and the client had no idea two deployments can be live at once. `PorterRatings.importAggregates` carries stars — without it an upgrade left deliveries intact and reputation zero, a half-erased record that looks plausible. `scripts/migrate.ts` enumerates from `DriverRegistered`/`VenueRegistered` (there is no on-chain list) and **reads every record back**, because a batch that silently skipped looks exactly like one that worked. The client half is the one that corrupts rather than fails: order ids are per-contract, so during a drain window **#7 exists on both contracts as two unrelated orders** and a stored record would resolve to whichever address the app held — showing a customer a stranger's delivery with a live Cancel button. `OrderRecord.at` is now stamped at creation and carried through every customer read and write; `at.test.ts` walks the source and fails if a call site drops it. `resolveFromRouter()` settles the address map once before first render, so an upgrade reaches phones that have not rebuilt. `test/migration-rehearsal.test.ts` is the rehearsal the plan demanded — real deliveries, real stars, all three registries upgraded, then completeness, idempotence, no-clobber and stake-stays-behind. **Rehearsed on Hardhat only; nothing has been migrated on Passet Hub.** No contracts redeployed, so live order state survived this one. 217 contract tests, 210 web tests. |
| 2026-09-24 | Migrated drivers, venues and ratings to successors | `bafybeifiiwow53cwlvykp2yxdmqo3tddhuamckaw6ebjbb4xotkotmnqt4`. The first real use of the migration path. drivers `0xaA7C…9818` → **`0xAC313449612f0099141c4ba8a3641aD4102e48e1`**, venues `0x0F10…3f0e` → **`0x0Fce9d4Ae1758EBEc87E061759FB60a530AbEcF1`**, ratings `0x34f9…3223` → **`0x7646a438e664692a91E87B53272971B05bD066b3`**. All three predecessors frozen against new entries; 14 drivers, 15 venues and 6 star aggregates carried across and verified by read-back against a snapshot taken beforehand. Orders, settlement and disputes re-pointed *after* the import, so there was never a window where a registered driver was invisible to orders. 18 validation checks pass. Cost 0.32 PAS. **Two things only the real chain showed.** (1) *The event log is not the list*: venue #6 is live on chain with a `PickupRecorded` event of its own, and its `VenueRegistered` event is **absent from the RPC's log index** — an events-based enumeration drops it and reports success. Venues are now enumerated from storage (`1..nextVenueId-1`); drivers, which have no on-chain enumeration, use every address in any event plus `EXTRA_DRIVERS`, narrowed by asking the contract. (2) *Pagination must be adaptive*: 15 venues estimated at ~220k gas and still failed with PolkaVM's `OutOfGas` — the per-transaction **proof size**, not gas. `BATCH = 50` reverted; `inPages` now halves a page until it fits and split 15 into 8+7 on its own. The run died on that revert, which is how `upgrade-registries.ts` gained the ability to resume from the router instead of redeploying. 218 contract tests, 210 web tests. |
| 2026-09-24 | Splash screen, message tray, button tiers, a mark | `bafybeiedubxmslf6gzrhxnbgsp2j4ovfqgcqgljf3ulslrdersndzrfauu`. **The white screen.** `index.html` was an empty `<div id="root">`, so nothing could paint until 2.2 MB of one-chunk JavaScript had come over IPFS, parsed and run — and the previous publish made it worse by awaiting `resolveFromRouter()` (4 s) *before* first render. The splash is now inlined in the document — markup, styles and the mark — so it paints from the first bytes; `main.tsx` moves the bar at four real milestones (bundle parsed → account → contracts → ready) and names each, so a stall stalls somewhere legible, with a message after 20 s saying what to do. A vite plugin inlines the 8 KB stylesheet, because a `<link>` in the head is render-blocking and was costing a second round trip before the splash could show at all. **The message tray.** Threads were rendered inside whichever screen owned them, so a reply arriving while you were elsewhere was silently missed, and the toggles that revealed them said "messages" in the same small text as everything else. Screens now *offer* a conversation (`views/Tray.tsx`) and the tray owns it: docked on every screen, tabs when there is more than one, unread counts that are honest because the threads stay mounted and listening whether the sheet is open or not. **Button tiers.** 47 `.link` buttons meant going back, refreshing, removing a tax line and opening a past order all looked identical. Now `.primary` → outlined → `.link`, plus `.back`, `.refresh` and `.danger` for the three patterns that kept recurring; destructive actions are marked by a leading × rather than by colour, since the accent *is* this palette's only alarm colour. Step navs became real tabs (`role="tablist"`, `aria-selected`) marked by weight and an underline instead of an accent fill that competed with the screen's primary action. **The mark:** a load carried, in strokes of `currentColor` so one asset serves both themes. Four tries — a big load straight on the fork of the legs reads as a sawhorse; a torso stroke plus an offset, well-tilted load is what makes it read as carrying rather than as a very square head. `splash.test.ts` fails if the two hand-kept copies drift. 218 web tests. |
| 2026-09-24 | Split the bundle: 817 → 183 kB gzipped at startup | `bafybeibpcsqspwx4iv4tpbwsiml77cunmq4ft6neo7t62us2cqlimlobhu`. The splash made the wait legible; this shortens it. **Measured first, and the guess was wrong**: snarkjs was already lazy (the 455 kB `browser.esm` chunk), and the biggest thing in the startup bundle was **poseidon-lite at 609 kB** — pulled in by the message tray published an hour earlier, via `Tray → Thread → order/chat → shield/notes → shield/pool`. Three changes. (1) The five role screens are `React.lazy` (`views/roles.ts`); `main.tsx` preloads the *saved* role behind the splash as a fifth milestone, so a returning device never sees a fallback. Buttons warm their chunk on `pointerdown`, so the download overlaps letting go. (2) The tray fetches `Thread` on demand — a conversation can only be offered from inside a role screen, which is a download of its own already. (3) **Poseidon per-arity**: the package barrel re-exports all sixteen arities and each drags its own round-constant table; the app uses 1, 2 and 3. `poseidon-lite/poseidon2` instead of `poseidon-lite` took the shared chunk from 683 kB (466 gz) to 98 kB (49 gz). Also lazy: `QrLazy.tsx` (jsQR + generator, 154 kB) and the note-backup panel (177 kB), so a driver no longer downloads the scanner before knowing the job needs one. **Startup: 1,762 kB → 593 kB raw, 817 → 183 kB gzipped.** Opening as a driver fetches 963 kB total where the old single bundle was 2.2 MB. Verified by loading the build from a CID-shaped subpath with a preset role: every chunk resolved, no 404s but the browser's own favicon. `shield/poseidon.test.ts` pins fixed hash vectors computed from the barrel *before* the import changed — nothing else in the suite would have caught a silent change, and a changed Poseidon strands every note already committed. 223 web tests. |
| 2026-09-24 | 100 live orders, every lever, and a cost model that was wrong | **98/100 ended as planned in 15.9 minutes** on Passet Hub — 70 delivered (68 ok), 8 cancelled open, 6 cancelled after assignment, 6 abandoned, 2 timed out and reopened, 4 disputes ruled for the customer, 4 for the driver. Levers: the relay fee curve rises 0.0075 → 0.01375 → 0.02 PAS across its 30 s climb and stops at the cap; 5 PAS of driver earnings shielded into five 1 PAS notes; the books froze tax with its rate on every sale and exported both CSV shapes; the pause registry refused new orders while paused and ran again after. Total gas for the whole run **8.01 PAS across 762 transactions**. Run recorded in `fixtures/fleet-run.json`. **The cost model was overstating everything by ~10×.** `costs.mjs` read `gas-snapshot.json`, which the Hardhat suite measures on an **EVM**, and multiplied it by Passet Hub's gas price — but these contracts run on **PolkaVM through pallet-revive**, which charges very different gas for the same work: `createOrder` 196,701 → **21,008** (9.4×), `confirmDropoffZK` 213,317 → **17,005** (12.5×), `insertShieldNote` 715,796 → **27,058** (26.5×), `ratings.rate` 107,665 → **6,227** (17.3×). "Gas, all three sides, steady state" was 0.9928 PAS (9.0% of a delivery); measured, it is **0.0747 PAS (0.7%)**. `tools/gas-live.mjs` now derives a table from a real run and `costs.mjs` prefers it, saying plainly when it has fallen back to the snapshot. **The two failures are a chain limit, not a contract bug**: both reverted with `gasUsed: 1905` and no revert reason, in the same block — PolkaVM refusing on **proof size**, the same limit that split the migration's venue import into 8+7. Two lanes' orders landed in one block and one was rejected. Worth knowing for a busy hour: concurrent orders against the same contract can be refused for proof size, and a client should retry rather than surface it. Costs per delivery, measured: **customer 0.0623 PAS of gas, driver 0.0323, venue 0.0019**; the protocol charged **0.0107 PAS**, being 250 bps of the fare and nothing else. |
| 2026-09-24 | Retry a refusal, and take the order id from the receipt | `bafybeielt3amsup44wu7wvv643shaqqqihx7qblb34clbi7dakpm4ts6ja`. The two orders the fleet run lost were **PolkaVM refusing on proof size**, which is metered per block as well as per transaction: a transaction that would succeed alone is included, reverts, burns 1,905 gas and carries **no revert reason**, so a customer would have seen their order fail for nothing they could act on. `web/src/send.ts` sends again when that happens. Telling it apart from a contract saying no is the whole difficulty — retrying a real `require` turns one clear error into three slow ones — so it takes **both** no revert data **and** gas too low to have run anything (`cancelOpen`, the cheapest real call, is 5,393); a `require(false)` with no message satisfies the first on its own and must not be retried. `send.test.ts` argues both directions from the receipt the run actually returned. Wired through `createOrder`, `acceptSealedBid`, `cancelOpen`, `reopenTimedOut`, `commitBid`, `confirmPickup`, `confirmDropoffZK`, `rate`, `openDispute` and `commitEvidence`; the customer reads "the chain is busy — placing it again" instead of watching a spinner that does not move. **A second bug the retry exposed**: `createOrder` read `nextOrderId` *before* sending, so two customers ordering in the same moment both read the same id and one would carry the other's order through the rest of the flow — the harness was fixed for this weeks ago and the app never was. The id now comes from the `OrderCreated` log. The harness gets the same retry and counts it, and a run smaller than the one in `fixtures/fleet-run.json` now writes beside it rather than over it: a 2-order rehearsal overwrote the hundred-order run once, and the average it left behind was not wrong so much as being passed off as something it was not. **Real gas for one delivery, measured: 71,711 (0.0717 PAS)** — `createOrder` 21,008, `commitBid` 10,367, `acceptSealedBid` 4,042, `confirmPickup` 13,062, `confirmDropoffZK` 17,005, `rate` 6,227. 232 web tests. |
| 2026-09-24 | Somewhere for a failure to stop | `bafybeibzqrfbrxqnpbg2kzfqujfitqr65p7s5lcos6bbpsfj23ewr2mzfq`. Reported from a phone: the driver's **Earnings** and the venue's **Takings** crash *app-wide* — not the tab, the whole app. The reason it is app-wide is that **there was no error boundary anywhere in the app**: React unmounts the entire tree when a render throws and nothing catches it, so any one panel could blank the session, with no tabs, no way back and nothing to read. `views/Boundary.tsx` now wraps the role screen, and each money panel separately, so a venue whose takings won't load can still export its books — and the error is *shown*, because the alternative is a blank screen that tells nobody anything. `Suspense` does not cover this: a lazy `import()` that **rejects** — a chunk that didn't arrive, ordinary enough over IPFS on a bad minute — is an error, not a suspension, and went straight past the fallback in `Funds`. **The root cause is not yet found, and this does not claim to fix it**; it contains it and makes it legible. What the search ruled out: no dangling chunk references in the build, no import cycles (so not a chunk-reshuffle TDZ), and all three panels mount clean in the states a device is in. **The deeper finding is why nothing went red**: 232 tests and *not one mounted a component* — `test: { environment: "node" }`, no DOM, no render coverage at all, which is how a screen that blanks the app shipped twice. `views/money.test.tsx` and `views/boundary.test.tsx` add happy-dom render tests per-file (`@vitest-environment`), mounting Earnings, Books and Funds in the empty state a first-time driver actually sees — the state a `[0]` or a `BigInt(undefined)` walks into. 240 web tests. |
| 2026-09-24 | The inlined stylesheet was also deleted, so lazy screens couldn't open | `bafybeidgp3kgqoqh6d72hzprqczf2ka5qdm7p4p7i4hbeq6jtb3442qjvu`. The boundary published an hour earlier did its job and named the cause: **"Unable to preload CSS for `assets/index-BOkboU74.css`"**. `inlineCss` in `vite.config.ts` folded the stylesheet into `index.html` and then ran `delete ctx.bundle[name]`, on the reasoning that an inlined stylesheet leaves an orphan behind. It does not. Vite records a CSS dependency on every chunk that needs one and `__vitePreload` fetches it *before* running the chunk, so with the file unpublished that fetch 404s and **the dynamic import rejects** — `Customer`, `Books` and `Funds` all declared it, which is exactly the set of screens that failed. The file is now published as well as inlined: one 8 KB request, made only when a lazy chunk loads and cached after, against a screen that otherwise cannot open. **Nothing could have caught this.** The typecheck passes — the reference is generated, not written. The tests pass — they do not build. The build passes — dropping an asset is what the plugin was told to do. It fails only on a device, on screens nobody opens until they have money in them. So `web/tools/check-dist.mjs` reads the built output, resolves every relative reference against the file that makes it, and fails the build if one is missing; it is wired into `npm run build` and was itself verified by deleting the stylesheet and watching it name all three chunks. Its first draft **silently passed that same deletion** — it stripped the `./` before capturing and so could no longer tell a real chunk dependency from a library's incidental `"package.json"`, which is worth recording as the failure mode of this kind of check. |
